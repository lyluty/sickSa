<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

  
  
<mapper namespace="sickSa.dao.TablesDao">

	<resultMap id="TablesResultMap"  type="sickSa.domain.Tables">
		<result property="tbl_id"  column="TBL_ID" />
		<result  property="tbl_isEmpty"  column="TBL_IS_EMPTY"/>
	</resultMap>

<select id="getTables"  parameterType= "java.lang.Integer"  resultMap="TablesResultMap">
	select * from TABLES where TBL_ID=#{tbl_id}
</select>

<insert id="insertTables"  parameterType= "sickSa.domain.Tables" >
	insert into TABLES (TBL_ID,TBL_IS_EMPTY)
				values(#{tbl_id},#{tbl_isEmpty})
</insert>


<update id="updateTables" parameterType="sickSa.domain.Tables">
	update TABLES 
			set 	TBL_IS_EMPTY=#{tbl_isEmpty}
			where TBL_ID=#{tbl_id}
</update>


<delete id="deleteTables" parameterType="sickSa.domain.Tables">
	delete from TABLES
			where TBL_ID=#{tbl_id} 
</delete>

</mapper>